@model IEnumerable<HospitalASP.Reception>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReceptionType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReceptionType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReceptionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReceptionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReceptionID })
        </td>
    </tr>
}

</table>
